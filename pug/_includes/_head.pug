
include ../_json/_meta.pug
- const id = meta[pageID].id
- const cssPath = meta.common.cssPath
- const pagesCssFiles = meta[pageID].cssFiles
- const pageName = meta[pageID].pageName
- const assetsPath = meta[pageID].assetsPath
- const description = meta[pageID].description
- const commonJsFiles = meta.common.jsFiles

//- console.log(cssPath)

head
  meta(charset="utf-8")
  meta(http-equiv="X-UA-Compatible", content="IE=edge,chrome=1")
  meta(name="viewport", content="width=device-width, initial-scale=1")
  meta(name="format-detection", content="telephone=no")
  meta(name="description", content=description)
  meta(name="keywords", content="")
  //css
  link(rel="stylesheet", href="//maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css")
  each item in cssPath
    link(rel="stylesheet" , href=assetsPath + item)
  each item in pagesCssFiles
    link(rel="stylesheet" , href=assetsPath + item)
  title #{pageName}
  //font-family
  script.
    (function(d) {
    var config = {
    kitId: 'pub7kpd',
    scriptTimeout: 3000,
    async: true
    },
    h=d.documentElement,t=setTimeout(function(){h.className=h.className.replace(/\bwf-loading\b/g,"")+" wf-inactive";},config.scriptTimeout),tk=d.createElement("script"),f=false,s=d.getElementsByTagName("script")[0],a;h.className+=" wf-loading";tk.src='https://use.typekit.net/'+config.kitId+'.js';tk.async=true;tk.onload=tk.onreadystatechange=function(){a=this.readyState;if(f||a&&a!="complete"&&a!="loaded")return;f=true;clearTimeout(t);try{Typekit.load(config)}catch(e){}};s.parentNode.insertBefore(tk,s)
    })(document);

